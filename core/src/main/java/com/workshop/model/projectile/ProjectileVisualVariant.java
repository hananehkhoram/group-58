package com.workshop.model.projectile;

/**
 * Semantic visual variants that cannot be derived from BulletType alone.
 *
 * This enum does not contain asset file names or paths. It only carries
 * gameplay information that is already known at projectile creation time.
 */
public enum ProjectileVisualVariant {
    DEFAULT,
    BUTTER,
    GIANT
}
