package controller.commands;

import controller.commandHandler.Command;

public class cheat_spawn_zombie implements Command {
    @Override
    public void execute(String[] args) {

    }

    //cheat spawn-zombie -t <zombie-type> -l <x, y>
}
